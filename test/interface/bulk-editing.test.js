describe('bulk-editing', () => {
  it.skip('should be able to edit a large file', () => {
    // go thru a large document like wealth of nations
    // tell the ai to replace every instance of a common word
    // see it come back with a precise diff
  })
  it.todo('edit a focused section without changing surrounding text')
})
