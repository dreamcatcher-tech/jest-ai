describe('messaging', () => {
  it.todo('send a message to another user')
  it.todo('send a solution to a user problem to them')
  /**
   * The output we want is a prompt that we can input against and get
   * the desired prompt response, but then have a file change so there
   * is some persistence.
   *
   * Being able to send a message to another user, perhaps describing how to
   * do something that the user was trying to do.
   * Involves discovery, then paying for the users agent to decide if the message
   * is relevant or not.
   *
   * Could we use the gpt calls to translate into chain format from git ?
   * Or translate into yaml ?
   *
   */
})
