describe('Fluid UI', () => {
  it.todo('be able to change the icon for the prompt')
  it.todo('collapse the history display to only show meaningful chats')
  it.todo('rearrange the UI based on a photo they sent in')
  it.todo('show an example to the user before confirming')
  it.todo('loop a bit to refine the result')
  it.todo('use dial commands where they can input changing amounts')
  // this should make a little function nugget for the user to call
  // so it should not consume any ai function calls
})
