describe('schema-loop', () => {
  it.todo('can develop a schema until it is error free')
  it.todo('can populate a schema with data until it is error free')
  it.todo('can realize it needs to extend the current schema')
})
