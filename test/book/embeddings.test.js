/**
 * the text is stored in an md file, and processed into a local db.
 * before using, the db is checked for completeness.
 * if the db is incomplete, the md file is processed again.
 * a thinking progess bar is shown while the db is being processed.
 * it prunes out anything that has changed.
 *
 * embeddings are tagged by filename, so human can refer to them in this way.
 *
 */
describe('book', () => {
  it.todo('store an embedding from a file')
})
